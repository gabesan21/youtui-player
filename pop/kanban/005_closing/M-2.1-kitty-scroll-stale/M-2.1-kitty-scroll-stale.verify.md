# Judgment — [[M-2.1-kitty-scroll-stale]]

- **Stage:** 005_closing (act 1) · **Owner:** Judge Dredd (single judge)

## Round 1 — 2026-09-11

- **Strategy:** differential — round 1, non-critical, size S; the whole diff (4 paths, +154/-24, commit 027c9d6) audited as the surface.
- **Surface:** `internal/ui/kitty.go`, `internal/ui/custom_list.go`, `internal/ui/AGENTS.md`, `pop/specs/ui-thumbnails.md`.
- **Returns:** execution 0 of 2 · plan 0 of 2.

### Objective and spec conformance

Card's What/Why: placements must follow content on scroll up and deletion — met by construction via content keying plus delete-then-fresh-id re-place; the generation safety net has one pinpoint defect (B1).

| # | Criterion | Mode | Result | Evidence |
|---|---|---|---|---|
| 1 | sinks keyed by content, never index | evidence | passed | custom_list.go:192 `prefix+":"+kittyContentKey(...)`; videoID from `/vi/` URL with track-URL fallback (custom_list.go:201-210); no `prefix:index` remains |
| 2 | changed spec deletes old placement, fresh ids | evidence | passed | kitty.go:288-309 — every re-place deletes first and allocates `nextPlacementID()`; fresh transmit only on path change, cached per path (kitty.go:387-395) |
| 3 | generation bump forces re-place of the list's sinks | evidence | failed | kitty.go:236-244 + 271-280 — bump consumed by the first sink per prefix; see B1 |
| 4 | `YOUTUI_KITTY_DEBUG` per-Sync log | evidence | passed | kitty.go:314-342 — env-gated desired/live keys, deletes, places, rects; unset → nil buffer, zero I/O |
| 5 | blocks path untouched | evidence | passed | no diff on the `tview.Image` path; `renderGen++` is inert for blocks |
| 6 | build/fmt/vet green; no new deps | evidence | passed | 004 telemetry green; diff touches no go.mod/go.sum; `slices` is stdlib (Go 1.24) |
| 7 | DOX + spec synced | evidence | passed | internal/ui/AGENTS.md content-keying bullet; spec invariants + debug error line coherent with code |
| 8 | real Kitty behavior | human | recorded | human checklist below |
| 9 | debug log aids field diagnosis | human | recorded | human checklist below |

### Implementation quality

| Severity | Finding | Evidence | Required correction |
|---|---|---|---|
| blocking | B1 — `generationBumped` records `r.generations[prefix] = gen` on the first sink of a prefix, so a render bump forces re-place of only ONE sink per list (whichever the random map iteration visits first); the list's remaining sinks skip the forced re-place and can keep a stale placement in exactly the equal-spec scenario the generation exists to heal — the debug log would also mislead (`force=true` on a single key) | kitty.go:236-244, 271-280 | evaluate the bump once per prefix before Sync's loops (e.g. `forced[prefix]` over `generations`, recording `r.generations`) and pass it to every `placeSink` of that prefix, both loops |
| suggestion | duplicate same-video rows in one visible window collide on the `prefix:videoID` key — the later spec overwrites the earlier and one row degrades to blank (the spec's designed degradation) | custom_list.go:192 | optional follow-up: disambiguate collisions (e.g. row-index suffix); not required for this fix |

### Scope and integration

- [x] Changes respect F01's `Owns`/`Must not edit` (4 paths, all owned; blocks behavior unchanged; no deps).
- [x] Consumers did not opportunistically implement dependencies.
- [x] No `verify: phase` criteria; `verify: user` criteria carried to the human checklist.
- [x] Affected specs, docs and DOX contracts are coherent.

## Verdict

- **Decision:** directed repair — one pinpoint blocker with an objective remedy and no strategy change.
- **Blocking findings:** B1 (criterion 3).
- **Plan defect:** none.
- **Suggestions/nits:** duplicate-video key collision (suggestion above).
- **Human checklist:** criterion 8 — in real Kitty, thumbnails track the cursor up/down in both lists; delete/move/page change leave no ghost images; `image_mode = "blocks"` is identical. Criterion 9 — if staleness persists, rerun with `YOUTUI_KITTY_DEBUG=/tmp/kitty.log` and confirm the log holds the bad frame's Sync decisions.
- **Summary:** the stale-placement bug class is eliminated by construction (content keys + fresh-id re-place); one pinpoint defect limits the generation safety net to a single sink per list.

## Delta of the return

- **Type:** execucao.
- **Affected criteria:** 3 — a generation bump must force re-place of all of the list's sinks, not just the first one visited.
- **Affected fronts:** F01.
- **Untouched fronts:** none (single front; the rest of F01 stays approved).
- **Expected action:** in kitty.go, compute the per-prefix bump once before Sync's loops and apply it to every sink of that prefix; all other behavior unchanged.

<!-- pop-verdict round=1 decision=reparo-dirigido -->
<!-- pop-delta round=1 kind=execucao pontual=true paths=internal/ui/kitty.go frentes=F01 intactas=none -->

### Directed-repair addendum — 2026-09-11 (same round 1)

- **Fixup audited by reading:** commit 5fa8c00, `internal/ui/kitty.go` (+19/−14) only.
- **B1 resolved:** the bump is resolved once per prefix before the sink loops (kitty.go:236-246) and both `placeSink` call sites receive `force[kittySinkPrefix(key)]` (kitty.go:255, 263); recording happens exactly once per prefix per Sync, "player" stays unforced, and the debug `force=%v` is now accurate per key.
- **Repair verdict:** resolved — criterion 3 passes; the round's decision becomes **aprovada**. Criteria 8–9 remain on the human checklist.

<!-- pop-verdict round=1 decision=aprovada -->
