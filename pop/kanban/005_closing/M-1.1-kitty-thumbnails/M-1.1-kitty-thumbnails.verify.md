# Judgment — [[M-1.1-kitty-thumbnails]]

- **Stage:** 005_closing (act 1) · **Owner:** Judge Dredd (single judge)

## Round 1 — 2026-09-10

- **Strategy:** full — round 1, no previously approved surface; non-critical M, single front F01, whole diff (11 paths, +508/-93) read once.
- **Surface:** `internal/config/{config.go,AGENTS.md}`, `internal/ui/{kitty.go(new),thumbnail.go,custom_list.go,setup.go,app.go,details.go,playlist.go,search.go,AGENTS.md}` vs `main...HEAD` (2 commits).
- **Returns:** execution 0 of 2 · plan 0 of 2.

### Objective and spec conformance

**Original request first:** the card's What/Why is met. Kitty terminals get real thumbnails (list + now-playing) via a hand-rolled protocol subset with `[ui] image_mode = auto|kitty|blocks`; every other environment keeps the pixelated `tview.Image` path untouched. Matches spec `ui-thumbnails` behavior and invariants (fallback guaranteed, no escapes in blocks mode, every placement has a matching delete, no new deps).

| # | Criterion | Mode | Verification | Result | Evidence |
|---|---|---|---|---|---|
| 1 | `image_mode` auto\|kitty\|blocks, default auto, unknown→auto | evidence | `internal/config/config.go:26,62,86,136-148` | passed | snake_case key in `[ui]`, default `"auto"`, `NormalizeImageMode()` maps unknown/empty to auto |
| 2 | Kitty only with TERM/KITTY_WINDOW_ID + config + no tmux | evidence | `internal/ui/kitty.go:31-42` | passed | all three gates in `kittyAvailable()`; blocks/tmux force fallback |
| 3 | No new dependencies | evidence | diff stat | passed | `go.mod`/`go.sum` untouched (11 paths, none of them) |
| 4 | Transmit from cache path, place by cell, delete by ID, blank cells, delete on scroll/page/resize/exit | evidence | `kitty.go` (a=T t=f, a=p c/r, a=d d=p); `setup.go:409-425`; `app.go:200-206` | passed | blank `tview.Box` owns cells; `syncKittyPlacements` reconciles per frame (scroll/page drop sinks); resize `Invalidate` in BeforeDraw; modal `Invalidate`; `DeleteAll` after `Run()` |
| 5 | Fallback intact | evidence | `custom_list.go:108-117`, `setup.go:104-113` | passed | blocks branch is byte-equivalent `tview.Image` config; sink selected per item/box |
| 6 | Cache cap raised, one path serves both sinks | evidence | `thumbnail.go:123-129,139` | passed | cap 100→640px with rationale comment; blocks sink reads the same files |
| 7 | Page change cannot paint stale thumbnail/placement | evidence | `custom_list.go:133-140,147-154`, `details.go:44-52`, `thumbnail.go:164-191` | passed | URL guard in `SetThumbnail`/`SetKittyThumbnail` at all 6 fetch sites via `fetchListThumbnail`; player box guarded by `thumbnailFetchStale` |
| 8 | Spike: real Kitty rendering, resize, clean exit | human | recorded for human checklist | recorded (user) | plan criterion 8 — run app in Kitty |
| 9 | List images track rows, clear on page change | human | recorded for human checklist | recorded (user) | plan criterion 9 — search/scroll/paginate in Kitty |
| 10 | DOX rules + contracts in sync | evidence | diff vs `internal/ui/AGENTS.md` | passed | UI mutations only via `QueueUpdateDraw`; escapes confined to main loop (documented in kitty.go header + AGENTS.md); `mu` discipline in `thumbnailFetchStale`; both AGENTS.md updated with hashes |

### Implementation quality

| Severity | Finding | Evidence | Required correction |
|---|---|---|---|
| — | none material | — | — |

Read with clean-code-review: correctness (placement reconciliation diffs desired vs live, unchanged placements not re-emitted), edges (empty URL, nil cache, zero rects, modal, resize), coupling (renderer is main-loop-only by design, stated in comments), names/comments (comments explain why, not what). Cursor-positioning interplay of `kittyPlace`'s CUP is exactly what user criterion 8 validates on real Kitty — not a reading defect.

### Scope and integration

- [x] Changes respect F01 `Owns` and `Must not edit` (`pop_check_scope` valid; `main.go`, `internal/search/**`, `go.mod/go.sum` untouched).
- [x] Consumers did not opportunistically implement dependencies.
- [x] No `verify: phase` criteria; `verify: user` criteria 8–9 recorded for the human checklist.
- [x] Spec `ui-thumbnails` and both DOX contracts coherent with the delivered code.

## Verdict

- **Decision:** approved → delivery and close-out.
- **Blocking findings:** none.
- **Plan defect:** none.
- **Suggestions/nits:** none material enough to record.
- **Human checklist:** criterion 8 — run the app in real Kitty: now-playing shows the real image below text, survives redraw/resize, no artifacts on exit; criterion 9 — search, scroll and paginate in Kitty: list images follow rows and leave no leftovers.
- **Summary:** asked for real Kitty thumbnails with config-gated fallback; got exactly that — detection, protocol primitive, placement lifecycle, stale-guard fix and cache resolution raise, with the blocks path intact.

<!-- pop-verdict round=1 decision=aprovada -->
