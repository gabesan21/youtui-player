---
task: 1.2.1-build-test-run-skills
entry: 02-arch-install-skill
---

# Arch install skill

Created `pop/skills/install-arch/SKILL.md` documenting `makepkg -si` and the local `make install-arch` / `make build` / `make install-bin` path, faithful to `PKGBUILD`/`.SRCINFO` and the personal-fork caveat. Round-1 directed repair fixed `make install-arch`: it installs deps only, not build.

## Evidence

- [[pop/skills/install-arch/SKILL.md]] — approved after round-1 addendum.
- [[PKGBUILD]] — upstream tarball, depends and makedepends.
