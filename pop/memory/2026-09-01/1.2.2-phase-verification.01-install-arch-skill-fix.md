---
task: 1.2.2-phase-verification
entry: 01-install-arch-skill-fix
---

# install-arch skill fix

Durante a verificação de Phase 1.2, o skill `pop/skills/install-arch/SKILL.md` foi revisitado. O target `install-arch` do `Makefile` executa `sudo pacman -S --needed mpv yt-dlp socat ffmpeg go`, ou seja, instala as dependências de runtime **e** a build dependency `go`. O texto anterior do skill omitia `go`; o diff ajustou a descrição para "installs runtime dependencies **and `go`** via `sudo pacman`".

## Evidence

- [[pop/skills/install-arch/SKILL.md]] — aprovado no 005_closing round 1.
- [[Makefile]] — target `install-arch` confirma `sudo pacman -S --needed mpv yt-dlp socat ffmpeg go`.
- Diff `9c8bb5d..3d9acc0` — alteração isolada em `pop/skills/install-arch/SKILL.md`.
