---
task: 2.1.3-remove-aur-publication
entry: 01-aur-metadata-removal
---

# AUR metadata removal

Deleted `.SRCINFO` (AUR metadata). `PKGBUILD`: maintainer `gabesan21`, local-install-only header, `url=` → fork, remote `source=`/`b2sums` removed, `prepare/build/package` build from `$startdir`. `README.md`: AUR badge removed; AUR install block (`yay -S`/`paru -S`) replaced with a local `makepkg -si` block.

## Evidence

- [[PKGBUILD]] — *local-tree build, no upstream tarball*.
- [[README.md]] — *local PKGBUILD install block*.
