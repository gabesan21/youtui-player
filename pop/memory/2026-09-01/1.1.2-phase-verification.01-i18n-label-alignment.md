---
task: 1.1.2-phase-verification
entry: 01-i18n-label-alignment
---

# Root i18n label alignment

Verified the root DOX rail and corrected a drift: `AGENTS.md` described languages as `pt-BR`/`PT-BR`, but the code uses `pt`. Changed the root rail to `pt`/`PT`.

## Evidence

- [[AGENTS.md]] — corrected labels.
- `internal/ui/i18n.go` defines `LanguagePT = "pt"`.
- `internal/config/config.go` normalizes `pt-*` to `"pt"`.

## Above-reach findings

- `README.md` and `pop/PROJECT.md` still list "PT-BR/EN"; fix belongs to a content task, not DOX.
