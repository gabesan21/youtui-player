---
task: 1.1.1-dox-tree-initialization
entry: 01-root-rail-refresh
---

# Root rail refresh

Validated the DOX root rail against the code and corrected drift: the `invidious.go` filename trap, Portuguese (`pt`) locale fallback, `search.SetTexts()` i18n bridge and the live test skipped under `-short`. Added a child-contract index for the three approved directories.

## Evidence

- [[AGENTS.md]] — *the refreshed root DOX rail*.
